

<%= simple_form_for @fav, html:{multipart: true } do |f| %>

  <%= f.input :title, input_html: { class: 'form-control'} %>
  <%= f.input :description %>
  <%= f.input :image, input_html: { class: 'form-control'} %>

  <%= f.button :submit %>

<% end %>
